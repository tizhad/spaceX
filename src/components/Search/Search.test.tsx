import React from "react";
import Search from "./Search";

describe("<Search />", () => {
  let component;

  beforeEach(() => {});

  test("It should mount", () => {
    expect(component.length).toBe(1);
  });
});
